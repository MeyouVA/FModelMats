<adonisControls:AdonisWindow x:Class="FModel.Views.MaterialGraphViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        WindowStartupLocation="CenterScreen" IconVisibility="Collapsed"
        PreviewKeyDown="OnWindowPreviewKeyDown"
        Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.75'}"
        Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.75'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="Material Graph Viewer"/>
        </Style>
    </adonisControls:AdonisWindow.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Padding="8 4"
                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                adonisExtensions:LayerExtension.IncreaseLayer="True">
            <WrapPanel Orientation="Horizontal">
                <TextBlock x:Name="PackageNameText" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0 0 16 0"/>
                <TextBlock x:Name="NodeCountText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="ConnectionCountText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <Button Content="Fit to View" MinWidth="78" Margin="4 0" Click="OnFitToView"/>
                <Button Content="Reset Zoom" MinWidth="78" Margin="4 0" Click="OnResetZoom"/>
                <Button x:Name="MaterialDetailsButton" Content="Material Details" MinWidth="100" Margin="12 0 4 0" Click="OnShowMaterialDetails"
                        ToolTip="Show every serialized property of the material, its instance chain and its shader map"/>
                <Button x:Name="ShadersButton" Content="Shaders" MinWidth="78" Margin="4 0" Click="OnShowShaders"
                        ToolTip="List every compiled shader in the shader map with its full disassembly"/>
                <ToggleButton x:Name="ExpandMathButton" Content="Expand Shader Math" MinWidth="120" Margin="12 0 4 0"
                              Visibility="Collapsed" Checked="OnExpandMathToggled" Unchecked="OnExpandMathToggled"
                              ToolTip="Replace the opaque 'Pixel Shader Math' boxes with the real compiled math — one node per decoded GPU instruction, including texture sample UV wiring"/>
                <ToggleButton x:Name="HighlightUserGraphButton" Content="Highlight User Graph" MinWidth="130" Margin="12 0 4 0"
                              Visibility="Collapsed" Checked="OnHighlightUserGraphToggled" Unchecked="OnHighlightUserGraphToggled"
                              ToolTip="Dim the engine shader scaffolding so the user-authored material nodes (everything feeding a material output attribute) stand out. View only — the graph is not changed."/>
                <ToggleButton x:Name="UserGraphOnlyButton" Content="User Graph Only" MinWidth="115" Margin="4 0"
                              Visibility="Collapsed" Checked="OnUserGraphOnlyToggled" Unchecked="OnUserGraphOnlyToggled"
                              ToolTip="Hide the engine shader scaffolding entirely and show only the user-authored material nodes wired into the material output, the way the graph looked in the editor."/>

                <Border Width="1" Margin="12 2" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

                <TextBlock Text="Isolate:" VerticalAlignment="Center" Margin="0 0 4 0" Opacity="0.75"/>
                <ComboBox x:Name="IsolatePinCombo" MinWidth="130" Margin="0 0 4 0" VerticalAlignment="Center"
                          SelectionChanged="OnIsolatePinChanged"
                          ToolTip="Show only the nodes that feed one material output pin. View only — the graph is not changed."/>
                <ToggleButton x:Name="DetectFunctionsButton" Content="Detect Functions" MinWidth="120" Margin="8 0 4 0"
                              Checked="OnDetectFunctionsToggled" Unchecked="OnDetectFunctionsToggled"
                              ToolTip="Highlight repeated sub-graph patterns. A material function inlined at cook time leaves an identical duplicated pattern each time it was used; matching colors mark those copies. View only."/>
                <Button x:Name="HideSelectedButton" Content="Hide (H)" MinWidth="70" Margin="8 0 4 0" Click="OnHideSelectedClicked"
                        ToolTip="Hide the selected nodes and their connections (keyboard: H). Adds to whatever is already hidden."/>
                <Button x:Name="UnhideAllButton" Content="Unhide All" MinWidth="80" Margin="4 0" Click="OnUnhideAllClicked"
                        ToolTip="Show every hidden node again (keyboard: Alt+H)."/>

                <Border Width="1" Margin="12 2" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

                <ToggleButton x:Name="ShowCodeButton" Content="Code View" MinWidth="86" Margin="4 0"
                              Checked="OnShowCodeToggled" Unchecked="OnShowCodeToggled"
                              ToolTip="Show the same recovered data as shader-style code: the compiled pixel shader's expressions per material output, with shared values hoisted, plus the decoded uniform expressions."/>
                <Button x:Name="PortToUnrealButton" Content="Port to Unreal" MinWidth="105" Margin="4 0" Click="OnPortToUnreal"
                        ToolTip="Write an Unreal editor Python script that rebuilds this material in a project. Operations with no equivalent Unreal node become Custom HLSL nodes rather than being approximated; the differences are listed before saving."/>
            </WrapPanel>
        </Border>

        <!-- Reconstruction notice -->
        <Border x:Name="ReconstructedBanner" Grid.Row="1" Padding="8 4" Visibility="Collapsed"
                Background="#403A2A00">
            <TextBlock x:Name="ReconstructedBannerText"
                       Text="Approximate reconstruction — the original node graph was stripped when this asset was cooked. Nodes show recovered parameters, textures and guessed connections."
                       TextWrapping="Wrap" Foreground="#FFE0B860"/>
        </Border>

        <!-- Main content: Graph + Properties panel -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <!-- Graph canvas -->
            <Grid Grid.Column="0">
                <Border x:Name="CanvasHost" ClipToBounds="True" Background="#FF181818"/>

                <!-- Code view: the same recovered data as text, over the graph rather than in a tab -->
                <Border x:Name="CodePanel" Visibility="Collapsed" Background="#FF1E1E1E">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Padding="8 5"
                                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Shader Code" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock x:Name="CodeStatsText" VerticalAlignment="Center" Margin="12 0 0 0" Opacity="0.7"/>
                                <Button Content="Copy" MinWidth="52" Padding="6 1" Margin="12 0 0 0" Click="OnCopyCode"
                                        ToolTip="Copy the whole listing to the clipboard"/>
                                <Button Content="Save…" MinWidth="58" Padding="6 1" Margin="6 0 0 0" Click="OnSaveCode"
                                        ToolTip="Write the listing to a text file"/>
                                <TextBlock Text="Word wrap" VerticalAlignment="Center" Margin="16 0 4 0" Opacity="0.7"/>
                                <CheckBox x:Name="CodeWrapCheck" VerticalAlignment="Center" Click="OnCodeWrapToggled"/>
                                <TextBlock x:Name="CodeCreditText" VerticalAlignment="Center" Margin="18 0 0 0" Opacity="0.7"/>
                            </StackPanel>
                        </Border>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <TextBox x:Name="CodeText" IsReadOnly="True" BorderThickness="0"
                                     Background="Transparent" Foreground="#FFD8D8D8"
                                     FontFamily="Cascadia Mono, Consolas, Courier New" FontSize="12"
                                     TextWrapping="NoWrap" AcceptsReturn="True" VerticalScrollBarVisibility="Disabled"
                                     HorizontalScrollBarVisibility="Disabled" Padding="10 8"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

            <!-- Node properties panel -->
            <Border Grid.Column="2"
                    Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                    adonisExtensions:LayerExtension.IncreaseLayer="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="8 6"
                            Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
                        <TextBlock x:Name="PropertiesTitleText" Text="Properties" FontWeight="SemiBold" FontSize="13"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PropertiesPanel" Margin="8 4"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Padding="8 4"
                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                adonisExtensions:LayerExtension.IncreaseLayer="True">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ZoomText" Text="Zoom: 100%" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="ParentChainText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="SelectedNodeText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="HiddenCountText" VerticalAlignment="Center" Margin="0 0 16 0" Opacity="0.7"/>
                <TextBlock Text="LMB drag: select / move  •  Shift+click: add  •  RMB/MMB drag: pan  •  Wheel: zoom  •  H: hide sel.  •  Alt+H: unhide all"
                           VerticalAlignment="Center" Opacity="0.55"/>
            </StackPanel>
        </Border>
    </Grid>
</adonisControls:AdonisWindow>
