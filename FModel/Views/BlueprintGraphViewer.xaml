<adonisControls:AdonisWindow x:Class="FModel.Views.BlueprintGraphViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FModel.Views.Resources.Converters"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        WindowStartupLocation="CenterScreen" IconVisibility="Collapsed"
        PreviewKeyDown="OnWindowPreviewKeyDown"
        Height="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenHeight}, Converter={converters:RatioConverter}, ConverterParameter='0.75'}"
        Width="{Binding Source={x:Static SystemParameters.MaximizedPrimaryScreenWidth}, Converter={converters:RatioConverter}, ConverterParameter='0.75'}">
    <adonisControls:AdonisWindow.Style>
        <Style TargetType="adonisControls:AdonisWindow" BasedOn="{StaticResource {x:Type adonisControls:AdonisWindow}}">
            <Setter Property="Title" Value="Blueprint Graph Viewer"/>
        </Style>
    </adonisControls:AdonisWindow.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Padding="8 4"
                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                adonisExtensions:LayerExtension.IncreaseLayer="True">
            <WrapPanel Orientation="Horizontal">
                <TextBlock x:Name="PackageNameText" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0 0 16 0"/>
                <TextBlock x:Name="NodeCountText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="ConnectionCountText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <Button Content="Fit to View" MinWidth="78" Margin="4 0" Click="OnFitToView"/>
                <Button Content="Reset Zoom" MinWidth="78" Margin="4 0" Click="OnResetZoom"/>
                <Button x:Name="DetailsButton" Content="Blueprint Details" MinWidth="110" Margin="12 0 4 0" Click="OnShowDetails"
                        ToolTip="Show the class, parent and the list of decompiled functions"/>
                <ToggleButton x:Name="ShowCodeToggle" Content="Show Code" MinWidth="82" Margin="4 0" Click="OnShowCodeToggled"
                              ToolTip="Show the decompiled statement text on the nodes. Off = compact editor-style nodes; the statement is always available in the properties panel."/>

                <Border Width="1" Margin="12 2" Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

                <TextBlock Text="Function:" VerticalAlignment="Center" Margin="0 0 4 0" Opacity="0.75"/>
                <ComboBox x:Name="FunctionCombo" MinWidth="180" Margin="0 0 4 0" VerticalAlignment="Center"
                          SelectionChanged="OnFunctionChanged"
                          ToolTip="Show only the graph of one function. View only — the graph is not changed."/>
                <Button x:Name="HideSelectedButton" Content="Hide (H)" MinWidth="70" Margin="8 0 4 0" Click="OnHideSelectedClicked"
                        ToolTip="Hide the selected nodes and their connections (keyboard: H). Adds to whatever is already hidden."/>
                <Button x:Name="UnhideAllButton" Content="Unhide All" MinWidth="80" Margin="4 0" Click="OnUnhideAllClicked"
                        ToolTip="Show every hidden node again (keyboard: Alt+H)."/>
            </WrapPanel>
        </Border>

        <!-- Reconstruction notice -->
        <Border Grid.Row="1" Padding="8 4" Background="#402A3A00">
            <TextBlock Text="Control-flow reconstruction from compiled Kismet bytecode — one node per statement, edges are the real jump/branch/flow targets. The editor's node layout is not stored in cooked assets, so positions are auto-arranged; drag to rearrange."
                       TextWrapping="Wrap" Foreground="#FFC0D860"/>
        </Border>

        <!-- Main content: Graph + Properties panel -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="320" MinWidth="220"/>
            </Grid.ColumnDefinitions>

            <!-- Graph canvas -->
            <Border x:Name="CanvasHost" Grid.Column="0" ClipToBounds="True" Background="#FF161616"/>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BorderBrush}}"/>

            <!-- Node properties panel -->
            <Border Grid.Column="2"
                    Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                    adonisExtensions:LayerExtension.IncreaseLayer="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="8 6"
                            Background="{DynamicResource {x:Static adonisUi:Brushes.Layer2BackgroundBrush}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="PropertiesTitleText" Text="Properties" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Copy" MinWidth="50" Padding="6 1" Click="OnCopyPanel"
                                    ToolTip="Copy everything shown in this panel to the clipboard"/>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PropertiesPanel" Margin="8 4"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Padding="8 4"
                Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
                adonisExtensions:LayerExtension.IncreaseLayer="True">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ZoomText" Text="Zoom: 100%" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="ClassChainText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="SelectedNodeText" VerticalAlignment="Center" Margin="0 0 16 0"/>
                <TextBlock x:Name="HiddenCountText" VerticalAlignment="Center" Margin="0 0 16 0" Opacity="0.7"/>
                <TextBlock Text="LMB drag: select / move  •  Shift+click: add  •  RMB/MMB drag: pan  •  Wheel: zoom  •  H: hide sel.  •  Alt+H: unhide all  •  Ctrl+C: copy sel. node text"
                           VerticalAlignment="Center" Opacity="0.55"/>
            </StackPanel>
        </Border>
    </Grid>
</adonisControls:AdonisWindow>
